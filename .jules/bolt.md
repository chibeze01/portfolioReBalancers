## 2024-03-28 - [Backend Yahoo Finance Bulk Download]
**Learning:** The application was experiencing an N+1 problem in `analytics_service` and `rebalance_service` because `get_price(h.symbol)` was called in a loop for each holding in a portfolio, triggering individual HTTP requests to Yahoo Finance for every ticker. This was not ideal and caused serious performance bottlenecks when calculating portfolio summaries.
**Action:** Implemented `get_prices_batch` in `server/app/pricing/yahoo_provider.py` using `yfinance.download(symbols_string, period="1d")` to batch requests. Replaced individual fetches in domain services with upfront bulk fetches. When optimizing similar pricing operations, always check if bulk retrieval capabilities exist for the upstream API/provider.
## 2024-04-20 - [Efficient Frontier Pricing Fallback Optimization]
**Learning:** When fixing N+1 price lookups in domain services (like efficient frontier) using , it's critical to preserve the existing fallback abstraction (e.g., trying Yahoo Finance first, then falling back to ). Directly calling the Yahoo batch fetch and falling back to a singular  breaks testing environments that rely entirely on the stub provider.
**Action:** Always mirror the singular fallback logic in a local  function that attempts the primary batch provider and then uses the secondary batch provider (or singular fallback for missing keys) to preserve testability and architectural abstraction.
## 2024-04-20 - [Efficient Frontier Pricing Fallback Optimization]
**Learning:** When fixing N+1 price lookups in domain services (like efficient frontier) using get_prices_batch, it's critical to preserve the existing fallback abstraction (e.g., trying Yahoo Finance first, then falling back to stub_provider). Directly calling the Yahoo batch fetch and falling back to a singular _get_price breaks testing environments that rely entirely on the stub provider.
**Action:** Always mirror the singular fallback logic in a local _get_prices_batch function that attempts the primary batch provider and then uses the secondary batch provider (or singular fallback for missing keys) to preserve testability and architectural abstraction.
